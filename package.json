{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "book-search-to-notion",
  "title": "Book Search to Notion",
  "description": "Lets you search books in Raycast to import into a notion book database with all book properties.",
  "icon": "extension-icon.png",
  "author": "plutotom",
  "owner": "proctors",
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "search-book",
      "title": "Search book",
      "description": "Search books to add to notion",
      "mode": "view"
    }
  ],
  "tools": [
    {
      "name": "search-books-for-notion-ai",
      "title": "Search Books for Notion AI",
      "description": "Use AI to search for a book and add it's details to notion rather then you looking your self."
    }
  ],
  "preferences": [
    {
      "name": "notionApiKey",
      "title": "Notion API Key",
      "description": "Your Notion integration secret token",
      "type": "password",
      "required": true
    },
    {
      "name": "notionDatabaseId",
      "title": "Notion Database ID",
      "description": "The ID of your Notion book database",
      "type": "textfield",
      "required": true
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.93.2",
    "@raycast/utils": "^1.17.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "20.8.10",
    "@types/react": "18.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
